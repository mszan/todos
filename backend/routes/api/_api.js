const authorization = require('./authorization')
const users = require('./users')
const tasks = require('./tasks')
const publicStats = require('./public')

module.exports = {authorization, users, tasks, publicStats}